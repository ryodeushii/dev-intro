# TODO

- [ ] fill about page
- [ ] collect "requests"/"topics" for the most crucial topics
- [ ] arrange some contact pointn (email, bot in messengere, etc)

## Component (example)

import { useState } from "react";

{/* Import CSS modules */}
import styles from '../components/counters.module.css'

export const Counter = () => {
  const [count, setCount] = useState(0);
  return (
    <div>
      <button onClick={() => setCount(count + 1)} className={styles.counter}>
        Clicked {count} times
      </button>
    </div>
  );
};

<Counter />

## External Component (example)

import Counters from "../components/counters";

<Counters />
